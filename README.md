# Cursor Rules Manager

![Logo](resources/icon.png)

> **Новое в версии 1.2.2:**
> - Исправлена проблема с немедленной автосинхронизацией при активации расширения
> - Теперь при активации только устанавливается таймер автосинхронизации
> - Автосинхронизация запускается только по расписанию или при сохранении файлов правил

Расширение для Cursor AI, которое управляет синхронизацией правил с GitHub репозиторием. Поддерживает разделение на глобальные и локальные правила, автоматическое управление .gitignore, защиту от потери данных, настраиваемые уведомления и интеллектуальное сохранение настроек режима работы правил Cursor AI.

[![CI/CD](https://github.com/nyxandro/cursor-rules-manager/workflows/CI/CD/badge.svg)](https://github.com/nyxandro/cursor-rules-manager/actions)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Version](https://img.shields.io/badge/version-1.2.2-blue.svg)](https://github.com/nyxandro/cursor-rules-manager/releases)

## 📦 Скачать расширение

**[⬇️ Скачать последнюю версию .vsix](https://github.com/nyxandro/cursor-rules-manager/raw/master/releases/cursor-rules-manager-1.2.2.vsix)**

## 🚀 Быстрый старт

### Шаг 1: Создайте репозиторий с правилами
1. Создайте новый GitHub репозиторий для хранения правил Cursor AI
2. Загрузите в него папку `.cursor` со всем содержимым из вашего проекта
3. Убедитесь, что структура выглядит так:
   ```
   .cursor/
   └── rules/
       ├── my-project/          # Локальные правила (не синхронизируются)
       ├── core/                # Глобальные правила (синхронизируются)
       ├── database/            # Глобальные правила (синхронизируются)
       └── testing/             # Глобальные правила (синхронизируются)
   ```

### Шаг 2: Установите расширение
1. [Скачайте .vsix-файл](https://github.com/nyxandro/cursor-rules-manager/raw/master/releases/cursor-rules-manager-1.2.2.vsix)
2. Установите в Cursor/VSCode через Extensions → Install from VSIX

### Шаг 3: Настройте расширение
1. Откройте настройки Cursor/VSCode (Ctrl+,)
2. Найдите раздел "Cursor Rules Manager"
3. Укажите **Rules Repo Url** - ссылку на ваш репозиторий с правилами
4. Настройте **Exclude Patterns** - папки, которые не должны синхронизироваться (по умолчанию: `my-project`)
5. **Перезапустите IDE** для применения настроек

### Шаг 4: Авторизуйтесь в Git
1. Откройте терминал в Cursor/VSCode
2. Выполните команду: `git config --global user.name "Ваше имя"`
3. Выполните команду: `git config --global user.email "ваш@email.com"`
4. При первом push/pull GitHub попросит авторизацию - следуйте инструкциям

### Шаг 5: Безопасная первая синхронизация
1. Откройте палитру команд (Ctrl+Shift+P)
2. Выполните команду "Проверить статус первой синхронизации" для анализа ситуации
3. Выполните команду "Безопасная первая синхронизация" для безопасного слияния правил
4. При обнаружении конфликтов выберите подходящую стратегию:
   - **Локальные правила имеют приоритет** — ваши правила перезапишут удаленные
   - **Удаленные правила имеют приоритет** — удаленные правила перезапишут ваши
   - **Создать резервную копию** — создается резервная копия локальных правил перед синхронизацией

### Возможные проблемы:
- **"Не удается подключиться к GitHub"** → Проверьте авторизацию в терминале
- **"Репозиторий не найден"** → Проверьте URL в настройках и перезапустите IDE
- **"Нет прав доступа"** → Убедитесь, что репозиторий публичный или у вас есть права на приватный

## Возможности

- **Синхронизация правил**: Автоматическая синхронизация правил между локальным проектом и GitHub репозиторием
- **Разделение правил**: Локальные правила (например, `my-project`) остаются только в проекте, глобальные правила синхронизируются
- **Управление через команды**: Простые команды для загрузки, отправки и синхронизации правил
- **Статус правил**: Просмотр текущего состояния правил в проекте
- **Автоматическое управление .gitignore**: Расширение автоматически создает и обновляет `.gitignore` файл для правильного исключения правил
- **Защита от потери данных**: Защита от случайного удаления всего репозитория при отсутствии локальных правил
- **Настраиваемые уведомления**: Контроль над уведомлениями автосинхронизации и временем их отображения
- **Улучшенные коммиты**: Коммиты с датой и временем для лучшего отслеживания изменений
- **Сохранение настроек режима правил**: Интеллектуальное сохранение пользовательских настроек режима работы правил Cursor
- **Автосинхронизация**: Автоматическая синхронизация при сохранении файлов правил с настраиваемым интервалом
- **Безопасная первая синхронизация**: Защита от потери данных при первой синхронизации с выбором стратегии слияния
- **Резервное копирование**: Автоматическое создание резервных копий локальных правил перед синхронизацией

## Архитектура правил

- **Глобальные правила** — все папки внутри `.cursor/rules`, кроме тех, что указаны в `Exclude Patterns`. Они синхронизируются между проектами через GitHub.
- **Локальные правила** — папки, указанные в `Exclude Patterns` (например, `my-project`). Они не синхронизируются и остаются только в текущем проекте.

```
.cursor/
└── rules/
    ├── my-project/          # Локальные правила (не синхронизируются)
    ├── core/                # Глобальные правила (синхронизируются)
    ├── database/            # Глобальные правила (синхронизируются)
    └── testing/             # Глобальные правила (синхронизируются)
```

## Автоматическое управление .gitignore

Расширение автоматически создает и обновляет `.gitignore` файл в корне проекта для правильного исключения правил Cursor AI:

```gitignore
# Cursor Rules
# Исключаем все правила Cursor из основного репозитория
.cursor/rules/*

# Разрешаем синхронизацию указанных папок с основным проектом
!.cursor/rules/my-project/
!.cursor/rules/my-project/**
```

Это обеспечивает:

- Исключение всех правил Cursor из основного репозитория проекта
- Разрешение синхронизации указанных папок (например, `my-project`) с основным проектом
- Автоматическое обновление при изменении настроек исключений
- Сохранение структуры файла с пустыми строками и комментариями

## Безопасная первая синхронизация

При первой установке расширения и настройке синхронизации возникает важный вопрос: как безопасно объединить локальные правила с удаленными, не потеряв данные?

### Проблема
- У вас есть локальные правила, которые вы разрабатывали
- В репозитории тоже есть правила
- Нужно безопасно объединить их без потери данных

### Решение

Расширение предоставляет систему безопасной первой синхронизации:

#### 1. Анализ ситуации
Команда "Проверить статус первой синхронизации" анализирует:
- Наличие локальных правил и их количество
- Наличие удаленных правил и их количество  
- Конфликты имен файлов между локальными и удаленными правилами
- Рекомендуемую стратегию синхронизации

#### 2. Выбор стратегии
При обнаружении конфликтов предлагаются варианты:

- **Локальные правила имеют приоритет** — ваши локальные правила перезапишут удаленные
- **Удаленные правила имеют приоритет** — удаленные правила перезапишут ваши локальные
- **Создать резервную копию** — создается резервная копия локальных правил в `.cursor/rules-backup/` перед синхронизацией

#### 3. Автоматическое резервное копирование
- Резервные копии сохраняются в `.cursor/rules-backup/backup-[timestamp]/`
- Каждая копия содержит полную структуру локальных правил на момент синхронизации
- Резервные копии можно восстановить вручную при необходимости

### Сценарии использования

#### Сценарий 1: Только локальные правила
```
✅ Безопасно отправляем локальные правила в репозиторий
```

#### Сценарий 2: Только удаленные правила  
```
✅ Безопасно загружаем правила из репозитория
```

#### Сценарий 3: Есть и локальные и удаленные правила
```
⚠️ Нужна стратегия слияния:
- Выберите приоритет (локальные или удаленные)
- Создайте резервную копию для безопасности
- Выполните синхронизацию
```

#### Сценарий 4: Конфликты имен файлов
```
⚠️ Обнаружены файлы с одинаковыми именами:
- Выберите стратегию слияния
- Создайте резервную копию
- Разрешите конфликты вручную при необходимости
```

## Защита от потери данных

Расширение включает защиту от случайного удаления всего репозитория правил:

- **Проверка локальных файлов**: Перед удалением файлов из репозитория проверяется наличие локальных правил
- **Блокировка удаления**: Если локальная папка `.cursor/rules` пуста или отсутствует, удаление файлов из репозитория блокируется
- **Логирование**: Все действия защиты записываются в лог для отслеживания
- **Резервное копирование**: Автоматическое создание резервных копий перед критическими операциями

## Система сохранения настроек режима правил

Расширение интеллектуально управляет настройками режима работы правил Cursor (например, `alwaysApply: true`):

### При синхронизации (push/pull/sync)

- **Сохраняется состояние пользовательских настроек режима правила** (например, `alwaysApply: true`)
- Обновляется только содержимое самих правил (контент внутри файла), но строка с настройкой режима, которую вы указали вручную, не изменяется
- Это гарантирует, что ваши индивидуальные настройки режима для каждого правила не будут случайно перезаписаны при синхронизации с GitHub

**Пример:**

```yaml
---
alwaysApply: true
---
# Здесь содержимое правила
Описание, инструкции и т.д.
```

При синхронизации будет обновлено только содержимое после блока настроек, а строка `alwaysApply: true` останется такой, как вы её установили.

### При загрузке (pull) из GitHub

- **Файл правила полностью перезаписывается** вместе с настройками, как они были в репозитории
- Это сделано намеренно: если вы хотите сбросить настройки к состоянию из репозитория, используйте загрузку (pull)

### Итог

- **Синхронизация** — сохраняет ваши локальные настройки режима правила
- **Загрузка из GitHub** — полностью перезаписывает файл, включая настройки режима, для сброса к эталону из репозитория

Это позволяет гибко управлять своими настройками:

- Локальные изменения настроек не будут случайно потеряны при обычной синхронизации
- Если нужно сбросить настройки к состоянию из репозитория — используйте загрузку (pull)

## Настройки

### Основные настройки

- **Rules Repo Url** — URL репозитория с правилами
- **Global Rules Path** — путь к папке с глобальными правилами
- **Exclude Patterns** — список папок, которые считаются локальными и не синхронизируются

### Настройки автосинхронизации

- **Auto Sync Interval** — Интервал автосинхронизации (минуты)
- **Show Auto Sync Notifications** — Показывать уведомления при автосинхронизации
- **Notification Timeout** — Время показа уведомлений (мс)

### Retry-механизм для сетевых операций

При временных ошибках подключения к репозиторию (например, проблемы с сетью, недоступность GitHub, таймауты) расширение автоматически повторяет попытку синхронизации несколько раз с экспоненциальной задержкой. Это позволяет избежать случайных сбоев автосинхронизации.

Параметры retry настраиваются через настройки расширения:

- **retryMaxAttempts** — максимальное количество попыток (по умолчанию 3)
- **retryBaseDelay** — базовая задержка между попытками в миллисекундах (по умолчанию 1000)
- **retryMaxDelay** — максимальная задержка между попытками в миллисекундах (по умолчанию 10000)
- **retryBackoffMultiplier** — коэффициент экспоненциального увеличения задержки (по умолчанию 2)

Если все попытки неудачны, пользователь увидит стандартное сообщение об ошибке.

## Команды

- **Синхронизировать правила Cursor** — полная синхронизация: загружает правила из GitHub и отправляет локальные изменения.
- **Загрузить правила из GitHub** — загружает последние версии правил из GitHub репозитория.
- **Отправить правила в GitHub** — отправляет локальные изменения правил в GitHub репозиторий.
- **Показать статус правил** — отображает текущее состояние правил в проекте.
- **Проверить статус первой синхронизации** — анализирует локальные и удаленные правила для безопасной первой синхронизации.
- **Безопасная первая синхронизация** — выполняет первую синхронизацию с защитой от потери данных и выбором стратегии слияния.

## Установка

### Из .vsix файла

1. [Скачайте .vsix-файл из репозитория](https://github.com/nyxandro/cursor-rules-manager/raw/master/releases/cursor-rules-manager-1.1.0.vsix) или с [GitHub Releases](https://github.com/nyxandro/cursor-rules-manager/releases)
2. Установите .vsix файл в Cursor/VSCode:
   - Откройте Cursor/VSCode
   - Перейдите в Extensions (Ctrl+Shift+X)
   - Нажмите "..." → "Install from VSIX..."
   - Выберите скачанный .vsix файл

### Из исходного кода

1. Клонируйте репозиторий: `git clone https://github.com/nyxandro/cursor-rules-manager.git`
2. Установите зависимости: `npm install`
3. Скомпилируйте: `npm run compile`
4. Запустите тесты: `npm test`
5. Упакуйте расширение: `npm run package`
6. Установите .vsix файл в Cursor

## Разработка

### Требования для разработки

- **Node.js 18+** (рекомендуется последняя LTS версия)
- **npm 9+** или **yarn**
- **Git**
- **Cursor AI** или **Visual Studio Code**

### Настройка окружения

#### 1. Клонирование репозитория
```bash
git clone https://github.com/nyxandro/cursor-rules-manager.git
cd cursor-rules-manager
```

#### 2. Установка зависимостей
```bash
npm install
```

**Примечание:** Все необходимые зависимости (включая TypeScript, ESLint, VSCE) уже включены в `package.json` как devDependencies. Это обеспечивает:
- Совместимость версий для всех разработчиков
- Работу в Docker и CI/CD без дополнительной настройки
- Возможность сборки расширения сразу после клонирования

#### 3. Проверка установки
```bash
# Проверка версий
node --version
npm --version
npx tsc --version
npx eslint --version
npx vsce --version

# Компиляция
npm run compile

# Запуск тестов
npm test
```

### Команды разработки

```bash
# Установка зависимостей
npm install

# Компиляция TypeScript
npm run compile

# Запуск тестов
npm test

# Линтинг кода
npm run lint

# Сборка расширения (.vsix файл)
npm run package

# Очистка временных файлов
npm run clean

# Разработка с автопересборкой
npm run dev

# Обновление версии и сборка
npm run version:patch  # для patch версии (1.2.2 → 1.2.3)
npm run version:minor  # для minor версии (1.2.2 → 1.3.0)
npm run version:major  # для major версии (1.2.2 → 2.0.0)
```

### Отладка

1. Откройте проект в Cursor/VSCode
2. Нажмите **F5** для запуска в режиме отладки
3. Откроется новое окно Cursor/VSCode с расширением
4. Используйте команды в палитре команд (Ctrl+Shift+P)

### Структура проекта

```
cursor-rules-manager/
├── src/                    # Исходный код TypeScript
│   ├── extension.ts        # Основной файл расширения
│   ├── rulesManager.ts     # Логика управления правилами
│   ├── gitignoreManager.ts # Управление .gitignore файлом
│   └── test/               # Тесты
│       ├── extension.test.ts
│       ├── gitignore.test.ts
│       ├── runTest.ts
│       └── suite/
│           └── index.ts
├── out/                    # Скомпилированный JavaScript
├── resources/              # Ресурсы расширения
│   └── icon.png
├── releases/               # Готовые .vsix файлы
├── .github/                # GitHub Actions
├── package.json            # Зависимости и скрипты
├── tsconfig.json           # Конфигурация TypeScript
├── .eslintrc.json          # Конфигурация ESLint
└── README.md               # Документация
```

### Публикация новой версии

1. **Обновите версию и соберите расширение:**
   ```bash
   npm run version:patch  # или minor/major
   ```

2. **Проверьте, что файл создался в releases/:**
   ```bash
   ls releases/
   ```

3. **Создайте GitHub Release:**
   - Перейдите в GitHub → Releases → Create a new release
   - Укажите тег версии (например, v1.2.3)
   - Загрузите .vsix файл из папки releases/
   - Добавьте описание изменений

4. **Обновите ссылку в README.md:**
   - Измените ссылку на скачивание в разделе "Скачать расширение"
   - Обновите версию в заголовке

### Советы для разработчиков

#### Локальная разработка vs Глобальные инструменты

**Для этого проекта рекомендуется использовать локальные зависимости:**

✅ **Хорошо:**
- Все инструменты в `package.json` как devDependencies
- `npm install` устанавливает всё необходимое
- Работает одинаково на всех машинах
- Docker и CI/CD работают без настройки

❌ **Не рекомендуется:**
- Глобальная установка TypeScript, ESLint, VSCE
- Зависимость от глобальных версий
- Разные версии на разных машинах

#### Работа с Git

```bash
# Создание новой ветки для фичи
git checkout -b feature/new-feature

# Коммит изменений
git add .
git commit -m "feat: add new feature"

# Пуш в репозиторий
git push origin feature/new-feature

# Создание Pull Request
# Перейдите в GitHub и создайте PR из feature ветки в main
```

#### Тестирование

```bash
# Запуск всех тестов
npm test

# Запуск только unit тестов
npm run test:unit

# Запуск с покрытием (если настроено)
npm run test:coverage
```

### Troubleshooting

#### Проблемы с зависимостями
```bash
# Очистка и переустановка
rm -rf node_modules package-lock.json
npm install
```

#### Проблемы с компиляцией
```bash
# Очистка и перекомпиляция
npm run clean
npm run compile
```

#### Проблемы с тестами
```bash
# Проверка конфигурации тестов
npm run pretest
```

#### Проблемы с VSCE
```bash
# Проверка версии VSCE
npx vsce --version

# Принудительная переустановка
npm uninstall vsce
npm install vsce --save-dev
```

## Требования

- Cursor AI или Visual Studio Code
- Git
- Node.js 18+

## История изменений

См. [CHANGELOG.md](CHANGELOG.md) для подробной истории версий.

## Лицензия

MIT License - см. файл [LICENSE](LICENSE) для подробностей.

## Поддержка

Если у вас есть вопросы или проблемы:

1. Проверьте [Issues](https://github.com/nyxandro/cursor-rules-manager/issues)
2. Создайте новое Issue с описанием проблемы
3. Укажите версию Cursor/VSCode и расширения

### Автоматическая синхронизация

Расширение поддерживает автоматическую синхронизацию правил с GitHub. Интервал автосинхронизации можно задать в настройках (`autoSyncInterval`). По умолчанию синхронизация происходит раз в час.

**Уведомления автосинхронизации по умолчанию отключены** для избежания спама. Их можно включить в настройках (`showAutoSyncNotifications`).

Сообщения о запуске и завершении автосинхронизации выводятся в Output канал "Cursor Rules Manager".

### Важные особенности

- **Синхронизация не изменяет блок настроек правила Cursor AI** (`--- ... ---`), если вы его редактировали
- **Загрузка из GitHub всегда перезаписывает файл полностью**, включая блок настроек
- **Автоматическое управление .gitignore** предотвращает дублирование правил и сохраняет структуру файла
- **Защита от потери данных** активируется автоматически при отсутствии локальных правил
